# Seed Migrations

This folder stores versioned seed files managed by db-migrate.

## Commands
- Create seed: `npm run seed:create -- <seed-name>`
- Run seeds up: `npm run seed:up`
- Roll back one seed: `npm run seed:down`

Seed history is tracked in the `seeds` table.
